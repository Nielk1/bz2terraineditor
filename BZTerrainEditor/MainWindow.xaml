<mah:MetroWindow x:Class="BZTerrainEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BZTerrainEditor"
        xmlns:nodenetwork="clr-namespace:NodeNetwork.Views;assembly=NodeNetwork"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <mah:MetroWindow.Resources>
        <Style TargetType="TabItem" x:Key="VerticalTabHeader">
            <Setter Property="Padding" Value="4,8"/>
            <Setter Property="HeaderTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <ContentPresenter Content="{Binding}">
                            <ContentPresenter.LayoutTransform>
                                <RotateTransform Angle="270" />
                            </ContentPresenter.LayoutTransform>
                        </ContentPresenter>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </mah:MetroWindow.Resources>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_New Workflow" />
                <MenuItem Header="_Open Workflow" />
                <MenuItem Header="_Save Workflow" />
            </MenuItem>
        </Menu>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" MinWidth="100"/>
                <!-- Left panel width -->
                <ColumnDefinition Width="5"/>
                <!-- Splitter width -->
                <ColumnDefinition Width="*"/>
                <!-- Main content -->
            </Grid.ColumnDefinitions>

            <TabControl TabStripPlacement="Left" >
                <TabControl.Resources>
                    <Style TargetType="TabItem" BasedOn="{StaticResource VerticalTabHeader}" />
                </TabControl.Resources>
                <TabItem Header="Variables">
                    <xctk:PropertyGrid SelectedObject="{Binding YourObject}"/>
                </TabItem>
            </TabControl>

            <!-- Draggable splitter -->
            <GridSplitter Grid.Column="1"
                          Width="5"
                          HorizontalAlignment="Stretch"
                          VerticalAlignment="Stretch"
                          Background="Gray"
                          ShowsPreview="True"/>
            
            <nodenetwork:NetworkView x:Name="networkView"
                                     Grid.Column="2"/>
        </Grid>
    </DockPanel>
</mah:MetroWindow>
