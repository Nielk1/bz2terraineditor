<ToggleButton x:Class="BZTerrainEditor.Views.Controls.ArrowToggleButton"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:local="clr-namespace:BZTerrainEditor.Views"
              xmlns:controls="clr-namespace:BZTerrainEditor.Views.Controls"
              xmlns:converters="clr-namespace:BZTerrainEditor.Converters"
              Name="button">
    <ToggleButton.Resources>
        <converters:ColorToBrushConverter x:Key="ColorToBrushConverter"/>
    </ToggleButton.Resources>
    <ToggleButton.Template>
        <ControlTemplate TargetType="controls:ArrowToggleButton">
            <Viewbox StretchDirection="Both" Stretch="Uniform">
                <Grid Width="20" Height="20">
                    <Path Width="20" Height="12" Stretch="Fill" Stroke="{Binding ArrowUncheckedColor, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource ColorToBrushConverter}}" Fill="Transparent" StrokeThickness="3" Data="M 0,0 L 10,9.5 L 20,0 ">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsChecked, ElementName=button}" Value="True">
                                        <DataTrigger.EnterActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <ColorAnimation Storyboard.TargetProperty="(Path.Stroke).(SolidColorBrush.Color)"
                                                                    To="{Binding ArrowCheckedColor, RelativeSource={RelativeSource TemplatedParent}}"
                                                                    Duration="0:0:0.1"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.EnterActions>
                                        <DataTrigger.ExitActions>
                                            <BeginStoryboard>
                                                <Storyboard>
                                                    <ColorAnimation Storyboard.TargetProperty="(Path.Stroke).(SolidColorBrush.Color)"
                                                                    To="{Binding ArrowUncheckedColor, RelativeSource={RelativeSource TemplatedParent}}"
                                                                    Duration="0:0:0.1"/>
                                                </Storyboard>
                                            </BeginStoryboard>
                                        </DataTrigger.ExitActions>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                    <Grid.RenderTransform>
                        <RotateTransform CenterX="10" CenterY="10"/>
                    </Grid.RenderTransform>
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsChecked, ElementName=button}" Value="True">
                                    <DataTrigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(Grid.RenderTransform).(RotateTransform.Angle)" To="-90" Duration="0:0:0.1"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </DataTrigger.EnterActions>
                                    <DataTrigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(Grid.RenderTransform).(RotateTransform.Angle)" To="0" Duration="0:0:0.1"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </DataTrigger.ExitActions>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>
                </Grid>
            </Viewbox>
        </ControlTemplate>
    </ToggleButton.Template>
</ToggleButton>